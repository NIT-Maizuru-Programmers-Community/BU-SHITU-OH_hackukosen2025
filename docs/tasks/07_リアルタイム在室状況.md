# タスク07: リアルタイム在室状況

## 概要

部室に現在いる人をリアルタイムで表示する機能を実装する

## 優先度

- **6**（最後に実装）

## 要件 ID

- F-016: 在室状況表示

## 前提条件

- タスク02（認証機能）が完了していること
- NFC ログインシステムが稼働していること

## タスク一覧

### 1. 在室状況表示

- [ ] ホーム画面に在室者エリア追加
- [ ] 在室者アバター一覧表示
- [ ] 在室人数表示
- [ ] リアルタイム更新（Firestore リアルタイムリスナー）

### 2. 在室状況詳細画面

- [ ] `/members` ページ作成
- [ ] 在室者詳細一覧
- [ ] 入室時刻表示
- [ ] 滞在時間表示

### 3. 入退室ログ管理

- [ ] 入室処理（NFC ログイン時）
- [ ] 退室処理（NFC ログアウト or タイムアウト）
- [ ] 在室状態の管理

### 4. ステータス表示

- [ ] オンライン/オフライン表示
- [ ] 最終入室日時表示

## Firestore スキーマ

```typescript
// attendance コレクション
interface Attendance {
  oderId: string;
  displayName: string;
  status: 'in' | 'out';
  checkInTime?: Timestamp;
  checkOutTime?: Timestamp;
  updatedAt: Timestamp;
}

// users コレクション（追加フィールド）
interface User {
  // ... 既存フィールド
  isInRoom: boolean;
  lastCheckIn?: Timestamp;
}
```

## 画面一覧

| 画面パス   | 説明             |
| ---------- | ---------------- |
| `/`        | ホーム（在室者サマリー） |
| `/members` | 在室者詳細一覧   |

## コンポーネント

- `MembersInRoom`: 在室者表示エリア
- `MemberAvatar`: メンバーアバター
- `MemberStatus`: ステータスバッジ
- `MemberList`: 在室者一覧
- `MemberDetail`: メンバー詳細
- `StayDuration`: 滞在時間表示

## リアルタイム更新実装

```typescript
// Firestore リアルタイムリスナー
import { collection, query, where, onSnapshot } from 'firebase/firestore';

const useMembersInRoom = () => {
  const [members, setMembers] = useState<User[]>([]);

  useEffect(() => {
    const q = query(
      collection(db, 'users'),
      where('isInRoom', '==', true)
    );
    
    const unsubscribe = onSnapshot(q, (snapshot) => {
      const membersData = snapshot.docs.map(doc => ({
        id: doc.id,
        ...doc.data()
      }));
      setMembers(membersData);
    });

    return () => unsubscribe();
  }, []);

  return members;
};
```

## デザイン案

```
┌─────────────────────────────┐
│  🏠 今、部室にいる人 (3人)   │
│  ┌───┐ ┌───┐ ┌───┐         │
│  │👤│ │👤│ │👤│           │
│  │田中│ │鈴木│ │佐藤│         │
│  └───┘ └───┘ └───┘         │
│                             │
│  ▶ 詳しく見る               │
└─────────────────────────────┘
```

## ハードウェア連携

- NFC リーダーからのログイン信号受信
- 入室時: `isInRoom = true`, `checkInTime` 記録
- 退室時: `isInRoom = false`, `checkOutTime` 記録
- タイムアウト処理（一定時間後に自動退室）

## 成果物

- ホーム画面での在室者表示
- 在室者詳細ページ
- リアルタイム更新機能

## 関連タスク

- [02_認証機能.md](./02_認証機能.md)
- [05_レース機能.md](./05_レース機能.md)

